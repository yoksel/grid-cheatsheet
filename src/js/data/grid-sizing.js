import gridTemplateValues from './props/defining/values';
import repeat from './props/defining/repeat';
import namedLines from './props/defining/named-lines';

export default {
  title: 'Explicit grid sizing',
  desc: '',

  link: 'https://www.w3.org/TR/css3-grid-layout/#track-sizing',

  items: [
    gridTemplateValues,
    repeat,
    namedLines
  ]
};
